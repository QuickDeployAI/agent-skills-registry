# Square Connect API (curated subset) endpoint reference

Generated by openapi-2-agent-skills from the manifest's digest-pinned OpenAPI source (spec version 2025-06-18).
Do not edit by hand — re-run the importer to update.

## GET `/v2/catalog/list`

List catalog objects (items, variations, categories).

| Parameter | In | Required | Description |
| --- | --- | --- | --- |
| `types` | query | no | Comma-separated object types, e.g. ITEM,ITEM_VARIATION. |
| `cursor` | query | no | Pagination cursor. |

## GET `/v2/customers`

List customer profiles.

| Parameter | In | Required | Description |
| --- | --- | --- | --- |
| `cursor` | query | no | Pagination cursor. |
| `limit` | query | no | Max results per page (max 100). |
| `sort_field` | query | no | DEFAULT or CREATED_AT. |

## GET `/v2/inventory/counts/{catalog_object_id}`

Retrieve current inventory counts for a catalog object.

| Parameter | In | Required | Description |
| --- | --- | --- | --- |
| `catalog_object_id` | path | yes | Catalog object (variation) ID. |
| `location_ids` | query | no | Comma-separated location IDs to filter by. |

## GET `/v2/orders/{order_id}`

Retrieve an order, including line items and fulfillment state.

| Parameter | In | Required | Description |
| --- | --- | --- | --- |
| `order_id` | path | yes | ID of the order to retrieve. |

## GET `/v2/payments`

List payments taken by the account, most recent first.

| Parameter | In | Required | Description |
| --- | --- | --- | --- |
| `begin_time` | query | no | RFC 3339 start of the reporting period. |
| `end_time` | query | no | RFC 3339 end of the reporting period. |
| `cursor` | query | no | Pagination cursor from the previous response. |
| `limit` | query | no | Max results per page (max 100). |

## GET `/v2/payments/{payment_id}`

Retrieve details for a specific payment.

| Parameter | In | Required | Description |
| --- | --- | --- | --- |
| `payment_id` | path | yes | Unique ID of the payment. |

## POST `/v2/refunds` (write)

Refund a payment in full or in part.

> Destructive/write operation: summarize the exact change and get
> explicit user confirmation before calling.
